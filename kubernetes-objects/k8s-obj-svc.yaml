apiVersion: v1
kind: Service
metadata:
  annotations:
    kubectl.kubernetes.io/last-applied-configuration: |
  labels:
    app: grupo-paas
  name: grupo-paas
  namespace: grupo-conocimiento
spec:
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8080
  selector:
    app: grupo-paas
  type: ClusterIP
